/**
 * @namespace Subjects
 * @interface Teacher
 * @property {string} firstName
 * @property {string} lastName
 * @export Subjects.Teacher
 */
namespace Subjects {
    export interface Teacher {
        firstName: string,
        lastName: string,
    }
}
